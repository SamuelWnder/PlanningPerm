# Cloudflare Pages configuration
# https://developers.cloudflare.com/pages/framework-guides/nextjs/ssr/get-started/

name = "planningperm"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Pages deployment — no `main` entry needed here.
# The build command in the Cloudflare Pages dashboard should be:
#   npx @cloudflare/next-on-pages
# Output directory: .vercel/output/static  ← Cloudflare reads this
# Node.js version: 20
